# Compact Renderer example

This executable Renderer Plugin demonstrates the QuickJS registration contract and returns a declarative render tree. Load it during development with:

```bash
AGENTJOURNEY_PLUGIN_DEV_DIRS=$PWD/examples/plugins/compact-renderer pnpm dev
```
